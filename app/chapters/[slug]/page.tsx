import { PageHero, Shell } from "../../Interior";import { collegeLogos, legacyChapters, slugify } from "../../site-data";
export default async function Chapter({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const match=legacyChapters.find(([name])=>slugify(name)===slug);const name=match?.[0]??"SOS Chapter";const place=match?.[1]??"Location to be confirmed";const logo=collegeLogos[name];return <Shell active="chapters"><PageHero index="Chapter" kicker="Chapter archive" tone="green" title={<>{name}<br /><i>organizes here.</i></>} intro={`${place}. Part of the nationwide SOS chapter story.`}/><section className="chapter-profile"><div className="profile-art chapter-logo-art">{logo?<img src={logo} alt={`${name} logo`}/>:<img src="/brand/dragonfly.jpg" alt="SOS artwork"/>}</div><div><p className="overline">Campus chapter</p><h2>Built by students.<br />Connected nationally.</h2><dl><div><dt>Campus</dt><dd>{name}</dd></div><div><dt>Location</dt><dd>{place}</dd></div><div><dt>Status</dt><dd>Chapter archive</dd></div><div><dt>Network</dt><dd>Students Organize For Syria</dd></div></dl><p>Want to reconnect this campus or help build its next chapter? Reach out to the national SOS team.</p><a className="pill pill-green" href="/contact?topic=chapter">Ask about this chapter ↗</a></div></section></Shell>}
