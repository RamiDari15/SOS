import { PageHero, Shell } from "../../Interior";import { legacyChapters, slugify } from "../../site-data";
export default async function Chapter({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const match=legacyChapters.find(([name])=>slugify(name)===slug);const name=match?.[0]??"SOS Chapter";const place=match?.[1]??"Location to be confirmed";return <Shell active="chapters"><PageHero index="Chapter" kicker="Legacy network profile" tone="green" title={<>{name}<br /><i>organizes here.</i></>} intro={`${place}. This chapter appeared in the legacy SOS network; its active status, leadership, and current programming require verification.`}/><section className="chapter-profile"><div className="profile-art"><img src="/brand/dragonfly.jpg" alt="Red and cobalt dragonfly print artwork"/></div><div><p className="overline">Profile pending verification</p><h2>Built by students.<br />Connected nationally.</h2><dl><div><dt>Campus</dt><dd>{name}</dd></div><div><dt>Location</dt><dd>{place}</dd></div><div><dt>Status</dt><dd>Verification required</dd></div><div><dt>Year established</dt><dd>To be provided</dd></div></dl><p>Before launch, SOS should supply an approved chapter photograph, short story, public contact method, social accounts, current campaigns, and a working join link.</p><a className="pill pill-green" href="/contact">Ask about this chapter ↗</a></div></section></Shell>}
